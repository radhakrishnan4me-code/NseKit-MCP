# ──────────────────────────────────────────────────────
# NseKit-MCP Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ──────────────────────────────────────────────────────

# Bearer token for MCP client authentication (required for n8n)
# Generate a strong random token: openssl rand -hex 32
MCP_BEARER_TOKEN=changeme

# Server bind address (default: 0.0.0.0)
# MCP_HOST=0.0.0.0

# Server port (default: 8000)
# MCP_PORT=8000
